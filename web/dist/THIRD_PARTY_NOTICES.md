# Third-party notices

The production browser runtime has no third-party package dependency.

The production bundle is built with `vite@7.0.6` and its locked transitive
build dependencies. Vite and build-tool license metadata remain recorded in
`package-lock.json`; build tools are not loaded by the browser runtime.
