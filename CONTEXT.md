# 个人投研交易策略平台

面向单用户的本地投研、计划与复盘领域。该词汇表只记录领域概念及其边界，不描述技术实现。

## 标的与用户关注

**Security（证券）**：
具有连续法律与交易身份的可交易证券；不同市场或股份类别分别属于不同证券，简称或交易代码变化不改变其身份。
_避免使用_：公司、发行人、股票代码、Ticker

**WatchlistItem（观察项）**：
某个证券被用户纳入某个观察列表后形成的关注关系；它表达持续关注，不表示实际持有。
_避免使用_：自选股、候选持仓、虚拟持仓

**Position（持仓）**：
某一账户在特定时点对某个证券的实际持有敞口；它是账户相关的汇总状态，不是交易记录或单个持仓批次。
_避免使用_：观察项、交易、持仓批次

**AccountSnapshot（账户快照）**：
某一账户在明确截至时点冻结的现金、持仓及其数据质量边界；新信息形成新快照，不能覆盖旧快照。
_避免使用_：实时账户、交易流水、可变账户状态

**UserDeclaredAccountSnapshot（用户声明账户快照）**：
由用户手工声明现金与持仓形成、且未与完整券商导出或交易账本对账的账户快照；账户身份、截至时点、声明时间、来源类型与用户确认必需，每条持仓的证券身份和总数量必需；现金、可用/冻结数量与成本基础可以明确为未知，未知不等于零，并使依赖它们的组合能力分别降级。它能表达当前敞口，但不能证明历史交易、费用、税费、收益或持仓批次完整性。
_避免使用_：已对账账户快照、券商流水、完整账户历史

**AccountSnapshotRevision（账户快照修订）**：
在截至时点不变时，以新的不可变账户快照补充原先未知的信息或提高声明精度；它引用被修订快照并列明变化，不能覆盖原快照，既有计划与评估的引用也不随之漂移。
_避免使用_：覆盖更新、新时点快照、纠错

**AccountSnapshotCorrection（账户快照纠错）**：
用户明确声明旧快照在其原截至时点包含错误时形成的新不可变账户快照；它引用被纠错快照、保留纠错原因和变化，不能改写依赖旧快照形成的历史。
_避免使用_：修订、删除旧快照、静默替换

**AccountSnapshotDraft（账户快照草稿）**：
Codex 从用户提供的截图、自然语言数值或文件中提取并等待身份、截至时点、歧义和一致性校验的结构化候选；它不是账户快照，不能被计划、评估或复盘引用。
_避免使用_：用户声明账户快照、已确认账户状态、临时覆盖

**AccountSnapshotVersion（账户快照版本）**：
用户确认后形成的不可变账户状态版本，是账户在明确截至时点的权威观察真值；后续观察、修订或纠错形成新版本。
_避免使用_：账户草稿、实时账户、可变当前仓位

**EstimatedAccountState（估算账户状态）**：
由最新已确认账户快照与其后用户确认的执行记录确定性推导出的当前估算；它用于连续工作，不会升级为新的权威账户快照。
_避免使用_：已确认账户快照、券商对账结果、实时账户

**BrokerImportedAccountSnapshot（券商导入账户快照）**：
由可解析的券商导出文件形成并通过文件内当前状态一致性校验的账户快照；它可以证明该导出所表达的当前状态，但除非另有完整历史证据，不能证明历史流水、费用、税费、收益或持仓批次完整性。
_避免使用_：券商截图、用户声明账户快照、完整账户历史

## 研究与证据

**Fact（事实）**：
能够追溯到明确来源、发布日期、可用时间和获取时间的信息；未经这些时间与来源约束的信息不能称为事实。
_避免使用_：原始数据、估计、假设

**Assumption（假设）**：
无法直接观测或需要主观设定的模型输入，必须明确记录依据、范围和理由。
_避免使用_：事实、预测结果

**Forecast（推演）**：
从事实与假设出发，包含因果链、期限、领先指标、概率、失效条件和更新规则的可证伪判断。
_避免使用_：笼统观点、看多、看空、估值

**Valuation（估值）**：
依据明确事实与假设、由确定性计算形成的价值分析结果，包含公式、单位和敏感性边界。
_避免使用_：语言模型估算、目标价建议、推演

**ScenarioValuation（情景估值）**：
在同一驱动结构上分别计算 stress、base、improvement 三个互斥条件情景的估值结果；无校准证据时只表达条件结果，不附加概率权重。
_避免使用_：Bull/Base/Bear、目标价情景、任意百分比区间

**ValuationSimulationDecision（估值仿真适用性决定）**：
说明估值蒙特卡洛是否具备确定性锚、分布、依赖、约束和收敛条件的结论；未运行也是一个有理由的正式结果。
_避免使用_：蒙特卡洛结果、默认仿真、随机补数

**MarketPathDecision（市场路径适用性决定）**：
说明交易价格路径模型是否具备时点有效行情、市场状态、交易约束和校准样本的结论；它不表达内在价值、目标价或交易动作。
_避免使用_：估值仿真、价格预测、交易信号

**RecentTrendAssessment（近期走势评估）**：
基于冻结的近期行情与明确基准形成的确定性趋势状态，包含观察窗口、指标、证据和失效条件。
_避免使用_：市场路径、价格预测、交易建议

**ResearchRequest（研究请求）**：
一次研究执行的不可变意图，指定证券、截至时点、研究配置和输入数据范围；它不包含研究结论，也不代表平台工作流。
_避免使用_：研究报告、研究运行、工作流请求
**ResearchAnalysisPlan（研究分析计划）**：
由正式研究请求、研究评估计划和冻结数据能力编译的内部不可变分析分解，声明闭合依赖、输出合同、校验器及 required/supporting 身份；它不允许调用方构造节点或执行代码，也不等于平台工作流。
_避免使用_：WorkflowRun、DynamicWorkflow、调用方工作流、生成代码


**ResearchRun（研究运行）**：
针对一个确定研究请求形成的不可变执行结果，无论结果为完成、受限或失败都作为独立历史记录保留。
_避免使用_：研究请求、研究报告、工作流运行

**Evidence（证据）**：
具有主体、语义、时点、内容、来源和分类的最小不可变引用单元；它可以支撑判断，但不会自动把假设或估计升级为事实。
_避免使用_：数据快照、来源文件、无出处结论

**ResearchInputOrigin（研究输入来源类型）**：
研究或模型输入的认识状态，只允许 observed_official、observed_structured、derived、estimated、missing；它不把结构化数据或估算升级为官方事实。
_避免使用_：来源等级、数据质量、官方性

**BoundedEstimate（有界估算）**：
由冻结观测或版本化先验确定性产生、同时记录点值、上下界、估算器、校准范围、依据和失效条件的模型输入。
_避免使用_：猜数、默认值、事实、缺失填零

**CompleteResearchReport（完整投研报告）**：
所有必需研究章节均存在并分别给出结果或明确受限、阻断、未运行状态的报告；完整指结构完整，不表示每个数字都来自官方来源或每个模型都已运行。
_避免使用_：全数据报告、全方法报告、数据充分报告

**ResearchReviewItem（研究复核项）**：
研究运行中用于后续验证观点的观察变量、验证信号、失效条件和复核窗口；它只触发研究复核，不表达交易动作。
_避免使用_：Conditional Plan、条件交易计划、交易计划、计划规则

## 数据与市场状态

**DataSnapshot（数据快照）**：
在指定截至时点按明确数据版本冻结的不可变输入集合，用于复现研究、估值或评估。
_避免使用_：证据、实时数据、可覆盖缓存

**MarketSnapshot（市场快照）**：
基于一个确定数据快照，对特定证券或市场在特定时点的市场状态所作的不可变、可解释描述。
_避免使用_：行情数据、数据快照、图表截图

## 交易计划与检验

**InvestmentThesisVersion（投资论点版本）**：
围绕一个证券、明确截至时点和冻结证据形成的不可变分析对象；它描述可证伪命题、驱动、风险和失效条件，不包含交易动作。
_避免使用_：交易计划、交易策略、买卖理由

**StrategyDefinition（策略定义）**：
可复用且版本化的计划模板，规定参数合同、袖套结构、规则模板与冲突政策；它不是某个账户和证券上的具体计划。
_避免使用_：交易计划、自由公式、研究观点

**TradePlanDraft（交易计划草稿）**：
尚未确认且处于 OPEN 状态的交易计划候选；最终修订可以在一次用户明确确认后形成交易计划版本，但草稿本身不会被每日工作流评估。
_避免使用_：交易计划版本、当前计划、已启用计划、已确认草稿

**TradePlan（交易计划）**：
用户针对一个账户与证券确认的主计划，作为具体策略实例统领 core 与可选 grid 袖套；同一账户与证券最多一个启用中的主计划。
_避免使用_：研究复核项、买卖建议、交易策略、委托单

**PositionSleeve（持仓袖套）**：
主计划内对持仓用途和数量边界的明确分区；第一版只允许 core 与 grid，历史未分类内容只读保留为 legacy_unsleeved。
_避免使用_：独立交易计划、持仓批次、tactical

**TradePlanVersion（交易计划版本）**：
交易计划在一次用户确认时形成的不可变内容版本；后续修改产生新版本，旧版本始终保留。
_避免使用_：草稿覆盖、当前计划、计划评估

**PlanRule（计划规则）**：
隶属于某个交易计划版本、可被确定性评估的条件与决策含义；规则可以提示用户复核，但不能自动提交交易。
_避免使用_：研究复核项、交易指令、策略信号

**PlanEvaluation（计划评估）**：
某个交易计划版本相对于一个确定市场快照的不可变评估结果，说明规则是否触发、未触发、受阻或无法判断及其原因。
_避免使用_：交易执行、计划版本、投资建议

**DecisionTask（决策任务）**：
由一次冻结复核产生、需要用户处理且可跨复核持续存在的正式事项；它记录待处理条件，不是交易指令。
_避免使用_：订单、通知日志、一次性提示

**ExecutionRecord（执行记录）**：
用户明确声明某项实际执行后形成的不可变记录；它可以更新估算账户状态，但在券商证据匹配前仍是未核验声明。
_避免使用_：券商成交、账户快照、交易指令

**DisciplineReviewVersion（纪律复盘版本）**：
对一个 weekly 或 custom 期间内的任务、行为、执行、偏离与证据缺口形成的不可变复盘；它不改写当时计划或账户历史。
_避免使用_：固定周五任务、月度工作流、绩效评分

**PlanImpactAssessment（计划影响评估）**：
Agent 基于冻结证据对既有计划可能受何种影响形成的版本化分析；它不能确认、启用或修改计划。
_避免使用_：计划变更、计划评估、交易指令

**PlanChangeProposal（计划变更提案）**：
从计划影响评估派生、等待用户处理的结构化变更候选；接受提案只会创建或更新交易计划草稿。
_避免使用_：已确认计划版本、自动计划修改、计划激活

**PlanConfirmationChallenge（计划确认挑战）**：
把一个 OPEN 草稿的最终修订、内容哈希、规范差异和启用意图冻结为一次用户明确确认的交互对象；草稿再次修订会使旧挑战失效。
_避免使用_：Agent 授权、计划版本、通用登录挑战、阶段确认

**UserApprovalReceipt（用户批准回执）**：
Application 在验证用户确认挑战后写入的不可变决定证据，分别记录决定者、交互渠道与传输者；它不能由 Agent 身份替代用户决定。
_避免使用_：登录会话、Agent 授权、传输日志

**ActionLogEntry（行为日志条目）**：
用户对决策任务所作处理及其理由的不可变记录；它描述决定行为，不证明证券交易已实际执行。
_避免使用_：执行记录、普通成功日志、系统审计噪声

**Strategy（交易策略）**：
可执行、可回测且输入输出明确的信号、仓位、风险和执行规则组合；单个交易计划或自然语言观点不是策略。
_避免使用_：交易计划、研究观点、模型

**Backtest（回测）**：
严格遵守历史信息可用时间、交易规则、费用和滑点的策略历史模拟。
_避免使用_：历史拟合、事后复盘、未来数据验证

## 工作流与产物

**DynamicWorkflow（动态工作流）**：
依据任务、证券特征、数据可用性和风险约束选择并组合处理节点的版本化流程定义。
_避免使用_：LLM Prompt、固定脚本链、工作流运行

**WorkflowRun（工作流运行）**：
一次动态工作流执行的独立身份和可追溯历史，覆盖平台级节点及其状态，并引用过程中形成的研究运行、计划评估和产物清单。
_避免使用_：研究运行、定时任务、运行日志文本

**ManualPortfolioReviewRun（手动组合复核运行）**：
由用户手动触发、从上次成功复核截止点比较到当前选定完整 A 股交易 session 的正式运行；它冻结证据与逐持仓结果，失败运行不推进截止点。
_避免使用_：每日调度、盘中监控、纪律复盘

**ArtifactManifest（产物清单）**：
某次工作流运行或可恢复检查点所关联输入与输出产物的不可变目录，记录每项产物的身份、类别、来源和完整性。
_避免使用_：产物内容、文件夹列表、研究报告

## 图表

**ChartAnnotation（图表标注）**：
锚定到证券及市场时间/价格坐标的持久化语义标记；它具有稳定身份，编辑保留旧版本，删除只终止其有效状态而不抹除历史。
_避免使用_：屏幕像素、图表截图、交易计划、即时绘图状态
