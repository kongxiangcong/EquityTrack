# 研究任务

只使用明确来源或明确缺失形成统一 `as_of` 的 EvidenceSet。在数据库事务外区分事实、假设、估计与缺失，形成包含论点、反方、驱动、风险、证伪条件和不确定性的 InvestmentCase 候选，再调用 `research.commit`。候选不得包含估值数字、评级、目标价结论或交易动作。

非关键缺失继续流动，并在直接依赖判断处说明限制。不得虚构来源、数字或引用。
